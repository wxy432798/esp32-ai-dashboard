# Optional. If unset, /api/eink-dashboard is open.
# Set this to the same key compiled into the ESP32 firmware to require auth.
ESP32_API_KEY=

# Required for write endpoints such as POST /api/usage.
# Do not compile this into the ESP32 firmware.
ADMIN_API_KEY=
